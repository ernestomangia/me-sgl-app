@model ME.Libros.Web.Models.IvaViewModel

<h3 class="page-header">Nuevo IVA</h3>
@using (Html.BeginForm("Crear", "Iva", FormMethod.Post, new { id = "formIva", @class = "form-horizontal formEntidad" }))
{
    @Html.Partial("Shared/Form_Partial", Model)

    <div class="row">
        <div class="col-md-5">
            <div class="text-right">
                <a id="btnCancelar" class="btn btn-default" href="@Url.Action("Index")">
                    Cancelar
                </a>
                <input id="btnCrear" type="submit" value="Crear" class="btn btn-success" />
            </div>
        </div>
    </div>
}