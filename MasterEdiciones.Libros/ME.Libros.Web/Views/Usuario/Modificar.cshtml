@model ME.Libros.Web.Models.UsuarioViewModel

<h3>Modificar usuario</h3>
@using (Html.BeginForm("Modificar", "Usuario", FormMethod.Post, new { id = "formUsuario", @class = "form-horizontal formEntidad" }))
{
    @Html.Partial("Shared/Form_Partial", Model)
       @Html.HiddenFor(model => model.Id)

     <div class="row">
        <div class="col-md-10">
            <div class="alert alert-danger validationSummary @(ViewData.ModelState.IsValid ? "hide" : "")" role="alert">
                <a class="close" data-hide="alert" aria-hidden="true">×</a>
                <b>Mensajes:</b>
                @Html.Raw(HttpUtility.HtmlDecode(Html.ValidationSummary().ToString()))
            </div>
        </div>
    </div>
 
    <div class="row">
        <div class="col-md-3">
            <div class="col-md-6 col-md-offset-4">
                <button class="btn btn-danger" type="button" data-toggle="modal" data-target="#modalEliminarUsuario" title="Eliminar">
                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                    Eliminar
                </button>
            </div>
        </div>
        <div class="col-md-2">
            <div class="text-right">
                <a class="btn btn-default" href="@Url.Action("Index")" title="Cancelar">
                    <span class="glyphicon glyphicon-remove-circle" aria-hidden="true"></span>
                    Cancelar
                </a>
                <button type="submit" class="btn btn-warning" title="Modificar">
                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    Modificar
                </button>
            </div>
        </div>
    </div>
}

@Html.Partial("Eliminar")
<input type="hidden" id="idEntidad" value="@Model.Id"/>