@model ME.Libros.Web.Models.ZonaViewModel

<h3 class="page-header">Modificar zona</h3>
@using (Html.BeginForm("Modificar", "Zona", FormMethod.Post, new { id = "formZona", @class = "form-horizontal formEntidad" }))
{
    @Html.Partial("Shared/Form_Partial", Model)
    @Html.HiddenFor(model => model.Id)

    <div class="row">
        <div class="col-md-10">
            <div class="text-right">
                <button class="btn btn-danger" type="button" data-toggle="modal" data-target="#modalEliminarZona" title="Eliminar zona">
                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                    Eliminar
                </button>
                <a class="btn btn-default btnCancelar" href="@Url.Action("Index")" title="Cancelar">
                    <span class="glyphicon glyphicon-remove-circle" aria-hidden="true"></span>
                    Cancelar
                </a>
                <button type="submit" class="btn btn-warning" title="Modificar zona">
                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    Modificar
                </button>
            </div>
        </div>
    </div>

}

@Html.Partial("Eliminar")
<input type="hidden" id="idEntidad" value="@Model.Id" />

